name: cooling-twin

channels:
  - conda-forge

dependencies:
  - python=3.11
  - numpy
  - scipy
  - pandas
  - matplotlib
  - pip
  - pip:
      - psychrolib
      - ruptures
      - pyyaml
      - -e .
      - pytest
      - pytest-cov
      - mypy
      - types-PyYAML
      - ruff
      - SALib